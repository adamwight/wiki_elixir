cache:
  key: shared
  paths:
    - _build/
    - deps/
    - priv/plts/

image: elixir:latest

before_script:
  - mix local.rebar --force
  - mix local.hex --force
  - mix deps.get

mix:
  script:
    - mix deps.clean --unused
    - mix deps.update --all
    - mix git_hooks.run all
